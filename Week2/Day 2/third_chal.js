// calcTip(100, 0.20) ->120

function(calcTip) {
    return value
}

let calcTip = (bill, tipPerc) => {
    return (bill * tipPerk) + bill;
}

let totalBill = calcTip(100, 0.2);
console.log(totalBill);



let totalBill2 = calcTipTwo(100, 0.2);
console.log(totalBill2);

function calcTipTwo (bill, )